# FUTURE_ML_2
Build a support ticket classifier using NLP. Preprocess text, extract features, and train models (e.g., Naive Bayes, Logistic Regression). Classify tickets and assign priority (High/Medium/Low) for efficient support handling.
